import styles from './CreateFolderButton.module.scss';

const CreateFolderButton = () => {
  return <button className={styles['create-btn']}>Create folder</button>;
};

export default CreateFolderButton;
